steps:
  
  - block: "Request build docker image"

  - label: ":docker: build docker image for SWIFT 2.4.x"
    command:
      - "sh .buildkite/build.sh swift24"
    agents:
      - "icube-staging=true"